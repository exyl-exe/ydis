<UserControl x:Class="Whydoisuck.UI.GroupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Whydoisuck.UI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Name="GroupViewUC">
    <Grid>
        <TabControl>
            <TabItem Header="Graph">
                <local:GraphView x:Name="Graph" GraphViewSessionGroup="{Binding ElementName=GroupViewUC, Path=GroupViewSessionGroup, Mode=TwoWay}"></local:GraphView>
            </TabItem>
            <TabItem Header="Sessions">
                <local:SessionsView x:Name="Sessions" SessionViewGroup="{Binding ElementName=GroupViewUC, Path=GroupViewSessionGroup, Mode=TwoWay}"></local:SessionsView>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
