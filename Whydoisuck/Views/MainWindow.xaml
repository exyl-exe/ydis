<Window x:Class="Whydoisuck.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:navigationpanel="clr-namespace:Whydoisuck.Views.NavigationPanel"
        xmlns:currentlevel="clr-namespace:Whydoisuck.Views.CurrentLevel"
        xmlns:props="clr-namespace:Whydoisuck.Properties"
        mc:Ignorable="d"
        Title="{x:Static props:Resources.AppTitle}"
        WindowStyle="None"
        Style="{StaticResource WDISWindow}"
        WindowState="{Binding InitialState}"
        Width="860" Height="900">
    
    <DockPanel Background="{StaticResource AppBackgroundColor}">
        
        <navigationpanel:NavigationPanel  DockPanel.Dock="Left"
                                          DataContext="{Binding NavigationPanel}"/>
        
        <ContentControl Content="{Binding CurrentView}"/>

    </DockPanel>
</Window>
