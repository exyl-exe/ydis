<UserControl x:Class="Whydoisuck.Views.SelectedSession.SessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Whydoisuck.Views.SelectedSession"
             xmlns:common="clr-namespace:Whydoisuck.Views.CommonControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer>
        <StackPanel Orientation="Vertical"
                    Background="LightGoldenrodYellow">
            <local:SessionSettingsPanel></local:SessionSettingsPanel>
            <common:LevelGraph DataContext="{Binding Graph}"/>
            <common:LevelDatagrid DataContext="{Binding Datagrid}"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
