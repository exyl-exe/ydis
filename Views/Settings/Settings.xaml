<UserControl x:Class="Whydoisuck.Views.AppSettings.Settings"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Whydoisuck.Views.AppSettings"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Margin="15">
        <TextBlock>TODO Application settings</TextBlock>
        <Border Margin="0,0,0,15">
            <DockPanel>
                <TextBlock DockPanel.Dock="Left">TODO Scan frequency (higher value = better performance but less accuracy)</TextBlock>
                <Slider Minimum="5" Maximum="100"></Slider>
            </DockPanel>
        </Border>
        <Border Margin="0,0,0,15">
            <DockPanel>
                <Button DockPanel.Dock="Right">TODO</Button>
                <TextBlock Text="TODOSave files : C:\Programs\Saves"/>
            </DockPanel>
        </Border>
        <Border>
            <DockPanel>
                <CheckBox DockPanel.Dock="Right"
                          HorizontalAlignment="Center"
                          Style="{StaticResource FilterCheckboxStyle}">TODOAutomatically start when Geometry Dash is launched</CheckBox>
            </DockPanel>
        </Border>
    </StackPanel>
</UserControl>
